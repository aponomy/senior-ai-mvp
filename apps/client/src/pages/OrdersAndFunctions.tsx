export default function OrdersAndFunctions() {
  return (
    <div
      style={{
        minHeight: '100vh',
        background: 'linear-gradient(180deg, #0a0b0f 0%, #1a1b2f 100%)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '40px',
        color: 'white',
      }}
    >
      <h1>Beställningar och Funktioner</h1>
    </div>
  );
}
